# Design sources and archives — kept locally, not published
reference/

# Full-resolution screenshot originals and the CashIn deck. The web-sized
# copies the pages actually load live in assets/images/projects/.
assets/screenshots/

# Local tooling / editor config — only needed on this machine
.claude/
.vscode/
.idea/

# macOS
.DS_Store
._*
# Leading slash matters: unanchored "Icon?" also matches assets/icons/
# because git on macOS compares ignore rules case-insensitively.
/Icon?

# Node (unused by this site, but keeps stray installs out)
node_modules/
